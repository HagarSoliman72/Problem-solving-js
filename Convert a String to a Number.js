// Convert a String to a Number!

// Solution
const stringToNumber = function (str) {
  return Number(str);
};

// or
const string_To_Number = function (str) {
  return +str;
};

const string_Number = function (str) {
  return parseInt(str);
};
